void main(){
    int a = 9.0;
    int b =  12.4 - 3.4;
    int c = 10 * (2.8 - 3.8) + 11;

    printf(" %d \t %d \t %d \n", a, b, c);

    // golang permette la conversione implicita solo se il risultato di un operazione ha decimali nulli
}